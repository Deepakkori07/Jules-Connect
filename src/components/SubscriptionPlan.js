import React from 'react'
import NavBar from './NavBar'

export default function SubscriptionPlan() {
  return (
    <div> 
        <NavBar/>
        <p>Note: We can finalize the design once Fundbox API integration flow will be finalized</p>
    </div>
  )
}
